package com.flipkart.fdp.ml
import org.apache.spark.rdd.RDD

import org.scalatest.FunSuite


//import org.apache.spark.s
/**
 * Created by shubhranshu.shekhar on 18/07/16.
 */
class AlgebraicTransformSuite{
//  test("really simple transformation") {
//    val input = List("hi", "hi cloudera", "bye")
//    val expected = List(List("hi"), List("hi", "cloudera"), List("bye"))
//    assert(SampleRDD.tokenize(sc.parallelize(input)).collect().toList === expected)
//  }
//
//  test("really simple transformation") {
//        val input = List("hi", "hi holden", "bye")
//        val expected = List(List("hi"), List("hi", "holden"), List("bye"))
//        assert(tokenize(sc.parallelize(input)).collect().toList === expected)
//      }
//    def tokenize(f: RDD[String]) = {
//        f.map(_.split(" ").toList)
//      }
}
